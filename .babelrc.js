module.exports = {
    // configurações criadas por outras pessoas
    presets: [
        // https://babeljs.io/docs/plugins/preset-env/
        // JS mais recente - converte apenas as func que o browser não entende
        '@babel/preset-env',
        // convereter o JSX
        '@babel/preset-react',
    ]
}